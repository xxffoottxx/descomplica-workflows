═══════════════════════════════════════════════════════════════════════════
                    QUICK REFERENCE - COPY & PASTE COMMANDS
═══════════════════════════════════════════════════════════════════════════

📦 RESTART N8N (Required after docker-compose.yml changes)
───────────────────────────────────────────────────────────────────────────
cd "c:\Users\andre\Desktop\Descomplica demos\descomplica-demos"
docker-compose down
docker-compose up -d


✅ CHECK N8N STATUS
───────────────────────────────────────────────────────────────────────────
docker ps
docker logs n8n


🔍 VERIFY EVALUATIONS FOLDER MOUNTED
───────────────────────────────────────────────────────────────────────────
docker exec -it n8n ls -la /home/node/.n8n/evaluations


🧪 TEST HOTEL WEBHOOK (From Inside Container)
───────────────────────────────────────────────────────────────────────────
docker exec -it n8n curl -X POST http://localhost:5678/webhook/hotel-support \
  -H "Content-Type: application/json" \
  -d '{"message": "Hello", "session_id": "test-123"}'


🧪 TEST HOTEL WEBHOOK (From Host)
───────────────────────────────────────────────────────────────────────────
curl -X POST http://localhost:5678/webhook/hotel-support \
  -H "Content-Type: application/json" \
  -d "{\"message\": \"Hello\", \"session_id\": \"test-123\"}"


📊 GOOGLE SHEET COLUMN HEADERS (Copy to Row 1)
───────────────────────────────────────────────────────────────────────────
test_id	timestamp	category	intent	query	language	final_score	accuracy	helpfulness	tone_brand	robustness	passed	response_time_ms	workflow_success	critical_issues	overall_assessment


🌐 LINKS
───────────────────────────────────────────────────────────────────────────
n8n Interface:
http://localhost:5678

Your Google Sheet:
https://docs.google.com/spreadsheets/d/1n4xNqXGxLEPfs0R2Xl74JZAX1imG2HZW0r45Ez_YoZQ/edit


📝 ENVIRONMENT VARIABLES (Already added to docker-compose.yml ✅)
───────────────────────────────────────────────────────────────────────────
EVALUATION_CONFIG_PATH=/home/node/.n8n/evaluations/configs/hotel-support-config.json
EVALUATION_RESULTS_SPREADSHEET_ID=1n4xNqXGxLEPfs0R2Xl74JZAX1imG2HZW0r45Ez_YoZQ
EVALUATION_RESULTS_SHEET_NAME=Sheet1


🔧 RESTART SINGLE CONTAINER
───────────────────────────────────────────────────────────────────────────
docker-compose restart n8n


📜 VIEW N8N LOGS (Live)
───────────────────────────────────────────────────────────────────────────
docker logs -f n8n


🧹 CLEANUP (If needed)
───────────────────────────────────────────────────────────────────────────
# Stop all containers
docker-compose down

# Stop and remove volumes (WARNING: Deletes data!)
docker-compose down -v


═══════════════════════════════════════════════════════════════════════════
                              QUICK START
═══════════════════════════════════════════════════════════════════════════

1. Restart n8n:
   docker-compose down && docker-compose up -d

2. Wait 10 seconds, then open:
   http://localhost:5678

3. Add credentials:
   - Google Sheets OAuth2
   - Gmail OAuth2

4. Import workflow:
   Example workflows/AI Agent Evaluation Framework.json

5. Link credentials to nodes in workflow

6. Execute workflow and watch 54 tests run!

═══════════════════════════════════════════════════════════════════════════
